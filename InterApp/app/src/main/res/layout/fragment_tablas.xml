<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tablas Sincronizadas"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="#D32F2F"
        android:padding="16dp"/>

    <TextView
        android:id="@+id/tvMensajeVacio"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="⚠️ No se pudo sincronizar el esquema.\n\nEl servidor de pruebas requiere autorización (HTTP 401) para este endpoint. Esto ocurre porque el TokenJWT retornado por el login es null en el ambiente de pruebas."
        android:textColor="#666666"
        android:padding="16dp"
        android:visibility="gone"/>

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerTablas"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"/>

</LinearLayout>